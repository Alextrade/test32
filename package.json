{
  "name": "bot",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/index.js",  // Запускает скомпилированный JavaScript (для Render)
    "build": "tsc",                 // Компилирует TypeScript в JavaScript
    "dev": "ts-node src/index.ts"   // Запускает TypeScript напрямую для разработки
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
